apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: unifi-nonprod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'git@github.com:pmpaulino/homelab-argo-apps.git'
    targetRevision: main
    path: 'apps/unifi/overlays/nonprod'
    kustomize:
      # Optional Kustomize settings
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: unifi-nonprod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
