apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: unifi-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'git@github.com:pmpaulino/homelab-argo-apps.git'
    targetRevision: main
    path: 'apps/unifi/overlays/prod'
    kustomize:
      # Optional Kustomize settings
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: unifi-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
